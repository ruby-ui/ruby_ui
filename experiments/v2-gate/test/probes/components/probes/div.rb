module Probes
  # One element, attributes from the layer, content from the caller — the shape
  # of every 1.6 wrapper component (Dialog, DialogHeader, Button, ...).
  class Div < RubyUI::Base
  end
end
