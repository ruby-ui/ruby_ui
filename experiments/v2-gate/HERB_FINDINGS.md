# Herb findings — Phase 0 and Phase 1

Date: 2026-09-07
Lane: `Gemfile.herb` = rails `60eb5cb7…` (8.2.0.alpha) + reactionview 0.4.0 + herb 0.10.3 (arm64-darwin), `intercept_erb = true`, `validation_mode = :raise`, `external_template_mode = :fallback`. Control: `Gemfile.erubi`, same Rails, Erubi handler.

Policy (plan §4): the 1.6 snapshot is the reference. A Herb rejection becomes a row here, not an edit of the markup and not a relaxed validation mode.

Classification: **no finding** (Herb accepted what 1.6 markup needs), **expected** (Herb rejected what it is meant to reject), **note** (behaviour of the toolchain worth knowing; not a defect), **template adjustment** (1.6 markup had to change to compile — none so far), **upstream** (defect isolated outside RubyUI with a reproducer — none so far).

| # | Template / probe | Source | Result | Classification | Evidence |
| --- | --- | --- | --- | --- | --- |
| 1 | `<div <%= tag.attributes(component.attrs) %>>` as root of all 9 dialog/button sidecars and the probe components | `SecurityValidator` | Compiles: the `tag.attributes(...)` call shape is the validator's one exception in attribute position. Attributes for `""`, boolean, `style` hash, escaped `"<>&'` canonicalize identically on both lanes. | no finding | `test/probes/herb_probe_test.rb` (i); `test/golden/parity_test.rb` |
| 2 | `<%= render Probes::Div.new(id: "outer") do \|g\| %>…<% end %>` with `capture(self, &block)` | compiler (block detection via Prism) | Block argument received; output identical on both lanes. | no finding | herb_probe_test (ii) |
| 3 | Nested `<%= render … do %>` inside that block | compiler | Identical on both lanes. | no finding | herb_probe_test (iii) |
| 4 | **Control, ReActionView #57 shape**: component template → helper with block → `render "probe/wrapper", &block` → partial `<%= yield %>` | compiler + ActionView `_layout_for`/`capture` | Identical on both lanes; content lands inside the partial in order. #57 did **not** reproduce with 0.4.0 + 0.10.3 on the pinned ref for this shape. The layer does not use this path; this is a result for users' templates. | no finding (control) | herb_probe_test (iv); `tmp/probes/{herb,erubi}/issue57.txt` after a run |
| 5 | `<div><span></div>` | **parser** (`MissingClosingTag`, "Opening tag `<span>` at (1:6) doesn't have a matching closing tag") | herb lane: `Herb::Engine::CompilationError`, surfaced as `ActionView::Template::Error#cause`. erubi lane: renders the string as-is. | expected | herb_probe_test (b); `tmp/probes/herb/bad_nesting.txt` |
| 6 | `dialog_content.html.erb`: static `<button>` containing `<svg>` and `<span>` | `NestingValidator` (interactive-inside-button) | Accepted: svg/span are not interactive. | no finding | parity `dialog/*` |
| 7 | `<path … />` self-closing inside `<svg>` | parser | Accepted. | no finding | parity `dialog/*` |
| 8 | `viewBox`, `fill-rule`, `clip-rule` written as in the snapshot (parser-adjusted case) | parser | Accepted; the canonical form's HTML5 parse yields the same `viewBox`. | no finding | parity `dialog/*` |
| 9 | Sentinel view = the literal `dialog/default` snapshot, including `xmlns:xmlns="…"` and `click-&gt;…` character references | parser | Accepted; parity trivially holds (runner self-check). | no finding | parity `sentinel/dialog_default` |
| 10 | Ruby containing `"`, `<`, `>`, `&`, `'` inside `<%= … %>` (probe view `attr_probe`) | lexer | ERB content is opaque to the HTML lexer; compiles. | no finding | herb_probe_test (i) |
| 11 | `<%= render RubyUI::Button.new do %>Open Dialog<% end %>` — block open and close on one line | compiler | Compiles; captured content is exactly the text. | no finding | parity `dialog/default` |
| 12 | Handler registration timing | ReActionView railtie | The `:erb` handler is swapped in `config.after_initialize { on_load(:action_view) … }`, i.e. when `ActionView::Base` loads. Before that, `ActionView::Template.handler_for_extension(:erb)` returns Rails' default even on the herb lane. Always ahead of the first template compile in practice; a test that inspects the handler must load `ActionView::Base` first. | note | `test/lane_test.rb` |
| 13 | `render inline:` templates | `ReActionView::Template::LocalTemplate#local_template?` (read, not executed) | An inline template's identifier is not under `Rails.root`, so it is "external": compiled by Herb with `:raise`, and on failure **silently** recompiled by Erubi (`external_template_mode: :fallback`, one `Rails.logger.warn`). A Herb rejection in an inline test template would go unnoticed. Every test template in this app is therefore a file. | note; candidate question for upstream (should inline templates count as local?) | `config/initializers/reactionview.rb`; reactionview-0.4.0 `lib/reactionview/template/handlers/erb.rb`, `local_template.rb` |
| 14 | `render_in(view_context)` with arity 1 | **Rails pinned ref**, not Herb (`actionview/lib/action_view/template/renderable.rb`) | Deprecated: "Action View support for #render_in without options is deprecated. Change #render_in to accept keyword arguments." `Renderable#render` passes `locals:`. The layer's signature is `render_in(view_context, **, &block)`. | note (pinned ref) | `app/components/ruby_ui/base.rb`; actionview CHANGELOG at the ref ("Pass render options and block to calls to `#render_in`") |
| 15 | ReActionView `debug_mode` | railtie (read, not executed) | Defaults to on in development and appends to `config.assets.precompile`, which propshaft does not define. Set to `false`; `:raise` surfaces errors instead of the overlay. | note | `config/initializers/reactionview.rb` |
| 16 | Development boot on the herb lane | — | Boots with reloading; `gate/dialog_open` renders through `ReActionView::Template::Handlers::ERB`. | no finding | `RAILS_ENV=development bin/rails runner …` (2026-09-07) |

## Spike lane (`Gemfile.tags`): herb `main` at `4269f79f` (2026-09-07), Rails' own `ERB::Herb` handler + `ComponentTags::Visitor`

Not part of the gate (decision 13). Herb `main` still reports `Herb::VERSION = "0.10.3"`.

| # | Template / probe | Source | Result | Classification | Evidence |
| --- | --- | --- | --- | --- | --- |
| 17 | Any first render of a template on the tags lane | **herb main compile path**: `lib/herb/engine/runtime/journal.rb:369` calls `SecureRandom.hex(4)` for a journal entry id | The gate's pin counted that call and a component-minted id came out as `content00000002`. The runner now compiles with an unpinned render before the pinned one (`Gate.render`). 0.10.3 draws no randomness at compile time. | note (runner) | `test/test_helper.rb`; `layer_probe_test.rb` "generated id" |
| 18 | `<RubyUI::Dialog>…</RubyUI::Dialog>` and the five other Dialog scenarios written as component tags (`app/views/gate/*_tags.html.erb`) | `ComponentTags::Visitor` + `ConstantResolver` (handles `::`) | Rewritten to `render RubyUI::Dialog.new do … end` (body) / `render X.new` (self-closing); nested tags and text bodies fine; all six twins byte-identical to the 1.6 snapshots. The visitor needs no parser option for `::` (dot notation does). | works (main only) | `parity_test.rb` "written with component tags" ×6; compiled source printed by the smoke runner |
| 19 | `<RubyUI::DialogContent size="lg">` | visitor attribute typing | A plain attribute is a **String literal**: `size: "lg"`. `DialogContent::SIZES` has Symbol keys, so the size class is silently dropped (`class` ends `md:w-full`, no `max-w-2xl`). `<RubyUI::Button variant="outline">` works only because Button calls `to_sym`. Booleans have the same problem (`open="true"` is the String `"true"`, and `"false"` is truthy). The Vue-style directive `:size=":lg"`, `:open="true"` passes Ruby. | 2.0 API decision: components accept/coerce Strings, or users write `:attr="ruby"` | smoke runner output; `dialog_content_*_tags.html.erb`, `dialog_open_tags.html.erb` |
| 20 | Block with parameter (`do \|group\|`) | visitor | Not expressible: the visitor emits a bare `do`. ToggleGroup's `yield(self)` shape (Phase 3) cannot be written as a tag; the ERB form stays available. | limit (main) | `lib/herb/engine/component_tags/visitor.rb` `erb_block_node` |
| 21 | The 46 ordinary tests (ERB syntax) on the tags lane | Rails' `ActionView::Template::Handlers::ERB::Herb` at the pinned ref, subclassed | Rails' handler overrides `add_text`/`add_expression`/`add_code`/`add_postamble`, which main's compiler still calls; all 46 pass. Incidentally a preview of the plan's `core` lane (Rails' handler via `config.action_view.erb_implementation`), with main instead of 0.10.3. | no finding | `bin/rails test` on the tags lane: 52/52 |
| 22 | Building herb from git | `ext/herb/extconf.rb` + `lib/herb/bootstrap.rb` | Needs a git-sourced Prism in the same bundle (`gem "prism", github: "ruby/prism", tag: "v1.9.0"`); extconf generates templates and vendors Prism, then compiles both. Worked first try on macOS arm64 with Xcode clang. The visitor prints `[Herb] \`ComponentTags::Visitor\` is experimental` once per process. | note | `Gemfile.tags`, `log/bundle-tags.log` |
| 23 | `<div><span></div>` on main | parser | Raises `Herb::Engine::ParseError`, a `CompilationError` subclass new on main (0.10.3 raises `CompilationError` itself). Probe (b) holds on both. | expected | herb_probe_test (b) on the tags lane |

Validators actually exercised at 0.10.3: the parser (row 5), `SecurityValidator` (row 1), `NestingValidator` (row 6). `AccessibilityValidator` is a stub and `RenderValidator` is not run (plan §1); nothing here can speak for them.

Template adjustments so far: **none** — every 1.6 markup shape Dialog and Button need compiled unchanged. Upstream findings so far: **none**.
